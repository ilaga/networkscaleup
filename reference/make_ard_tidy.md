# Construct tibble from ARD matrix

Construct tibble from ARD matrix

## Usage

``` r
make_ard_tidy(ard)
```

## Arguments

- ard:

  the ARD matrix

## Value

a tibble of ARD, with columns for row/col index
